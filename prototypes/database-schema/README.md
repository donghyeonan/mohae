# Database schema visual prototype

Open `index.html` to inspect the static schema explainer.

The canonical schema document is [`../../docs/architecture/database-schema.md`](../../docs/architecture/database-schema.md).
